# 🧪 Environment Setup

This project uses **Chemprop (D-MPNN)**, RDKit, and standard scientific Python libraries.  
To ensure reproducibility, the environment can be created in two ways:

---

# 1. Conda Environment (Recommended)

## 🔧 Create environment
```bash
conda env create -f environment.yml
conda activate chemprop

python -c "import chemprop; print('Chemprop OK')"


2. Manual Installation (Alternative)

If you prefer to install packages manually:

2.1 Create base environment
conda create -n chemprop python=3.10 -y
conda activate chemprop

2.2 Install RDKit
conda install -c conda-forge rdkit -y

2.3 Install Chemprop (official GitHub)
pip install git+https://github.com/chemprop/chemprop.git

2.4 Install additional Python packages
pip install pandas numpy scikit-learn matplotlib seaborn tqdm
pip install jupyter notebook ipykernel

3. Verify Key Components
Check RDKit:
python -c "from rdkit import Chem; print(Chem.MolFromSmiles('CCO'))"

Check Chemprop CLI:
chemprop_train --help

Add Jupyter kernel:
python -m ipykernel install --user --name chemprop --display-name "chemprop"

4. (Optional) GPU Support for Chemprop

If using an NVIDIA GPU, install CUDA-enabled PyTorch:

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121


Check CUDA availability:

python -c "import torch; print(torch.cuda.is_available())"

5. Repository Structure (for reference)
AISD_coursework2_drug_discovery/
│
├── data/
├── notebooks/
├── chemprop_runs/
├── generative/
├── environment.yml
└── ENVIRONMENT.md


This environment is compatible with the folder layout above.

You're ready to run the baseline, contextualised models, and generative demo.


---

# 🎉 完成！

你現在可以 **直接複製這段放到 GitHub**，什麼都不用改。

---

如果你需要：

✅ 幫你產生 `environment.yml`（最推薦版）  
✅ 幫你產生 `requirements.txt`  
✅ 幫你改 README 的 Getting Started 區段  

告訴我就幫你補上！
